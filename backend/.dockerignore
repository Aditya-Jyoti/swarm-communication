# Build context is backend/. Keep it to what the Go build and the images need:
# go.mod, go.sum, cmd/, pkg/, deploy/. Everything else only slows context
# upload and busts caches.
*
!go.mod
!go.sum
!cmd/
!pkg/
!deploy/
**/*_test.go
**/testdata
